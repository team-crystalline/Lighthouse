const db = require('../db');

/**
 * This is a class for a BDA plan. For those not in the know: A BDA plan is a Before-During-After plan for preparing for potentially triggering or traumatic events. 
 */
class BDAPlan {
    constructor(dbResults) {
    }
}

module.exports = BDAPlan;