const db = require('../db');

/**
 * A class for a forum. These are inside a ForumCategory. They have a name, description, and the topics inside. Consider also a latest post property.
 */
class Forum {
    constructor(dbResults) {
    }
}

module.exports = Forum;