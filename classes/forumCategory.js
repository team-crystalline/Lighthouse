const db = require('../db');

/**
 * A class for a forum category. This is used to store information about a category, such as its name, description, and the forums it contains.
 */
class ForumCategory {
    constructor(dbResults) {
    }
}

module.exports = ForumCategory;