const db = require('../db');

/**
 * A class for a topic reply. These are inside a ForumTopic. They have a reply content, the author, and the timestamp. You will need to use a JOIN query to get the author of the reply.
 */
class ForumReply {
    constructor(dbResults) {
    }
}

module.exports = ForumReply;