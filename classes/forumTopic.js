const db = require('../db');

/**
 * A class for a forum topic. These are inside a Forum. They have a topic name, the original post, and the posts inside. You will need to use a JOIN query to get the OP of the topic.
 */
class ForumTopic {
    constructor(dbResults) {
    }
}

module.exports = ForumTopic;